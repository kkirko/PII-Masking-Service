# PII Masking Service - offline requirements for Windows (Python 3.11)
# System prerequisites (install separately, offline):
# - Python 3.11 x64 (includes pip and venv)
# - Microsoft Visual C++ Build Tools 2022 (only if wheels are missing)
#
# Offline install (prepare wheelhouse on an internet machine):
# python -m pip download -r requirements_pii.txt -d wheelhouse --platform win_amd64 --python-version 3.11 --only-binary=:all:
# Copy wheelhouse/ + this file to the offline PC, then:
# python -m pip install --no-index --find-links wheelhouse -r requirements_pii.txt
#
# Core runtime (imported in .py files)
fastapi==0.128.0
uvicorn==0.39.0
pydantic==2.12.5
cryptography==46.0.3
httpx==0.28.1
#
# Uvicorn performance/reload extras (recommended for local dev)
uvloop==0.22.1
httptools==0.7.1
watchfiles==1.1.1
websockets==15.0.1
#
# Optional (only if you want .env auto-loading or faster JSON)
python-dotenv==1.0.1
ujson==5.10.0
